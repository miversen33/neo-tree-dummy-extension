To use this, use the following basic configuration

```lua
require("neo-tree").setup({
    source_selector = {
        winbar = true,
    },
    sources = {
        "neo-tree-dummy-extension"
    },
})
```
